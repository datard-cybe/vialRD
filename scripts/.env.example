# VialRD ETL — Variables de entorno
# Copia este archivo a .env y rellena con tus credenciales reales
# NUNCA commitear el .env real al repo

# Project URL (la misma que el frontend)
SUPABASE_URL=https://bmazvbpclpeprgdiijhw.supabase.co

# Service Role Key — NO la anon key.
# Encuentra esta en: Supabase Dashboard → Settings → API → service_role secret
# Empieza con 'eyJ...' (es un JWT largo)
# OJO: nunca expongas esta key en frontend ni en repositorios públicos
SUPABASE_SERVICE_ROLE_KEY=eyJ_PEGA_AQUI_TU_SERVICE_ROLE_KEY
